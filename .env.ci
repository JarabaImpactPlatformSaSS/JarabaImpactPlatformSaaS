# ===========================================
# CI Environment Variables
# ===========================================
# Usado por GitHub Actions ci.yml:
#   cp .env.ci .env
#
# Estas variables son SOLO para el entorno de CI.
# No contienen secretos reales (los secretos se
# inyectan via GitHub Secrets en el workflow).
# ===========================================

# Drupal / Drupal Test
SIMPLETEST_DB="mysql://root:root@127.0.0.1:3306/drupal_test"
SIMPLETEST_BASE_URL="http://localhost"

# PHP
PHP_MEMORY_LIMIT="-1"

# AI Services (deshabilitados en CI)
OPENAI_API_KEY=""
QDRANT_HOST="localhost"
QDRANT_PORT="6333"

# Node Environment
NODE_ENV="test"
