{#
/**
 * @file
 * page--metodo.html.twig - Template para la landing del Método Jaraba.
 *
 * ARQUITECTURA:
 * - Zero Region Policy (ZERO-REGION-001) — usa {{ clean_content }}
 * - Reutiliza partials de header y footer
 * - Full-width, mobile-first layout
 * - SCSS: ecosistema_jaraba_theme/metodo-landing library
 *
 * RUTA: /metodo
 *
 * @see MetodoLandingController::landing()
 * @see ecosistema_jaraba_theme.theme para preprocess
 */
#}
{% set site_name = site_name|default('Jaraba Impact Platform') %}

{{ attach_library('ecosistema_jaraba_core/global') }}
{{ attach_library('ecosistema_jaraba_theme/metodo-landing') }}

<div class="page-wrapper page-wrapper--clean page-wrapper--landing page-wrapper--metodo">

  {# HEADER - Partial reutilizable con mega menú #}
  {% include '@ecosistema_jaraba_theme/partials/_header.html.twig' with {
    site_name: site_name,
    site_slogan: site_slogan,
    logo: logo,
    logged_in: logged_in,
    theme_settings: theme_settings,
    available_languages: available_languages|default([]),
    current_langcode: current_langcode|default('es'),
    language_prefix: language_prefix|default(''),
    language_links: language_links|default({}),
    mega_menu_columns: mega_menu_columns|default([]),
  } only %}

  {# MAIN CONTENT - Full width #}
  <main id="main-content" class="landing-main">

    {% if clean_messages %}
      <div class="highlighted container">
        {{ clean_messages }}
      </div>
    {% endif %}

    {# CONTENIDO DE LA PÁGINA (render del MetodoLandingController) #}
    {{ clean_content }}

  </main>

  {# FOOTER - Partial configurable desde UI #}
  {% include '@ecosistema_jaraba_theme/partials/_footer.html.twig' with {
    site_name: site_name,
    logo: logo,
    footer_copyright: footer_copyright,
    theme_settings: theme_settings,
  } only %}

  {# FAB COPILOTO #}
  {% include '@ecosistema_jaraba_theme/partials/_copilot-fab.html.twig' with {
    logged_in: logged_in,
    theme_settings: theme_settings,
  } only %}

</div>
