services:
  jaraba_credentials_cross_vertical.evaluator:
    class: Drupal\jaraba_credentials_cross_vertical\Service\CrossVerticalEvaluator
    arguments:
      - '@entity_type.manager'
      - '@jaraba_credentials.issuer'
      - '@logger.factory'

  jaraba_credentials_cross_vertical.activity_tracker:
    class: Drupal\jaraba_credentials_cross_vertical\Service\VerticalActivityTracker
    arguments:
      - '@entity_type.manager'
      - '@logger.factory'
