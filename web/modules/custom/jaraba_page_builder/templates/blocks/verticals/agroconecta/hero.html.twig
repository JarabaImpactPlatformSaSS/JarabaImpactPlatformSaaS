{#
 # Template: AgroConecta - Escaparate del Productor
 # Category: agroconecta
 # Type: hero
 #
 # Split layout: hero content (eyebrow + h2 + subtitle + CTAs) + media
 # Premium: animations, parallax media, background image, jaraba_icon system
 # - Usa jaraba_icon(category, name, { variant, size, color }) para iconos
 #}
<section class="pb-section pb-section--hero pb-section--agroconecta jaraba-block jaraba-block--premium"
         data-block-type="agroconecta_hero"
         aria-label="{{ title|default('Hero AgroConecta')|t }}"
         data-effect="fade-up"
         {% if background_image %}style="background-image: url('{{ background_image }}')"{% endif %}>
  <div class="pb-section__container">
    <div class="pb-section__hero-content">
      <span class="pb-section__eyebrow">{{ eyebrow|default('AgroConecta')|t }}</span>
      <h1 class="pb-section__title">{{ title|default('Del campo a tu mesa, sin intermediarios')|t }}</h1>
      <p class="pb-section__subtitle">{{ subtitle|default('Conecta directamente con productores locales y accede a productos frescos, orgánicos y de temporada.')|t }}</p>
      <div class="pb-section__actions">
        <a href="{{ cta_url|default('#') }}" class="pb-section__cta pb-section__cta--primary">
          {{ cta_text|default('Ver productos')|t }}
        </a>
        {% if cta_secondary_text is defined %}
          <a href="{{ cta_secondary_url|default('#') }}" class="pb-section__cta pb-section__cta--secondary">
            {{ cta_secondary_text }}
          </a>
        {% else %}
          <a href="#" class="pb-section__cta pb-section__cta--secondary">{{ 'Conocer productores'|t }}</a>
        {% endif %}
      </div>
    </div>
    <div class="pb-section__media" data-parallax="0.3">
      {% if image %}
        <img src="{{ image }}" alt="{{ title|default('')|striptags }}" loading="lazy" />
      {% else %}
        <div class="pb-section__img-placeholder" aria-hidden="true">
          {{ jaraba_icon('ui', 'seedling', { variant: 'duotone', size: '48px', color: 'primary' }) }}
        </div>
      {% endif %}
    </div>
  </div>
</section>
