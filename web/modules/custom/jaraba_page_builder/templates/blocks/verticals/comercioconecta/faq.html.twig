{#
 # Template: ComercioConecta - Envío y Devoluciones
 # Category: comercioconecta
 # Type: faq
 #
 # Header + accordion con <details>/<summary>
 #}
<section class="pb-section pb-section--faq pb-section--comercioconecta"
         aria-label="{{ title|default('Preguntas frecuentes')|t }}">
  <div class="pb-section__container">
    <div class="pb-section__header">
      <span class="pb-section__eyebrow">{{ eyebrow|default('FAQ')|t }}</span>
      <h2 class="pb-section__title">{{ title|default('Preguntas frecuentes')|t }}</h2>
      <p class="pb-section__subtitle">{{ subtitle|default('Resolvemos tus dudas sobre envíos, devoluciones y más.')|t }}</p>
    </div>
    <div class="pb-section__faq-list">
      {% set default_faqs = [
        { question: '¿Cuánto tarda el envío?', answer: 'Los envíos estándar tardan entre 2-5 días laborables. Ofrecemos envío express en 24h para pedidos urgentes.' },
        { question: '¿Puedo devolver un producto?', answer: 'Sí, tienes 30 días desde la recepción para devolver cualquier producto en su estado original. El proceso es sencillo desde tu panel.' },
        { question: '¿Cómo gestiono mi inventario?', answer: 'Desde el panel de control puedes añadir, editar y gestionar todo tu catálogo de productos con stock en tiempo real.' },
        { question: '¿Hay costes de envío?', answer: 'El envío es gratuito para pedidos superiores a 50€. Para importes menores, el coste de envío se calcula automáticamente.' },
        { question: '¿Puedo personalizar mi tienda?', answer: 'Por supuesto. Personaliza colores, logotipo, tipografía y la disposición de tu tienda para que refleje la identidad de tu marca.' }
      ] %}
      {% set items = faqs|default(default_faqs) %}
      {% for faq in items %}
        <details class="pb-section__faq-item">
          <summary>{{ faq.question|default('Pregunta')|t }}</summary>
          <div class="pb-section__faq-answer">
            {{ faq.answer|default('Respuesta.')|t }}
          </div>
        </details>
      {% endfor %}
    </div>
  </div>
</section>
