{#
 # Template: ServiciosConecta - Preguntas Frecuentes
 # Category: serviciosconecta
 # Type: faq
 #
 # Header + accordion con <details>/<summary>
 # Premium: animations, jaraba_icon system
 # - Usa jaraba_icon(category, name, { variant, size, color }) para iconos
 #}
<section class="pb-section pb-section--faq pb-section--serviciosconecta jaraba-block jaraba-block--premium"
         data-block-type="serviciosconecta_faq"
         aria-label="{{ title|default('Preguntas frecuentes')|t }}"
         data-effect="fade-up">
  <div class="pb-section__container">
    <div class="pb-section__header">
      <div class="pb-section__header-icon" aria-hidden="true">
        {{ jaraba_icon('ui', 'help', { variant: 'duotone', size: '48px', color: 'azul-corporativo' }) }}
      </div>
      <span class="pb-section__eyebrow">{{ eyebrow|default('FAQ')|t }}</span>
      <h2 class="pb-section__title">{{ title|default('Preguntas frecuentes')|t }}</h2>
      <p class="pb-section__subtitle">{{ subtitle|default('Resolvemos tus dudas sobre nuestra plataforma de servicios profesionales.')|t }}</p>
    </div>
    <div class="pb-section__faq-list">
      {% set default_faqs = [
        { question: '¿Cómo se verifican los profesionales?', answer: 'Cada profesional pasa por un proceso de verificación que incluye comprobación de titulaciones, colegiación activa, experiencia laboral y referencias de clientes anteriores.' },
        { question: '¿Qué garantías tengo al contratar?', answer: 'Todos los servicios contratados a través de la plataforma están cubiertos por nuestra garantía de satisfacción. Si no estás conforme, mediamos para encontrar una solución o te devolvemos el importe.' },
        { question: '¿Cómo funciona el sistema de pago?', answer: 'El pago se realiza a través de la plataforma de forma segura. El importe se retiene hasta que confirmas que el servicio ha sido completado satisfactoriamente.' },
        { question: '¿Puedo solicitar presupuesto sin compromiso?', answer: 'Por supuesto. Puedes solicitar presupuestos a varios profesionales de forma gratuita y sin compromiso. Cada presupuesto incluye un desglose detallado del servicio.' },
        { question: '¿Qué tipo de profesionales puedo encontrar?', answer: 'Contamos con consultores, abogados, contables, asesores fiscales, arquitectos, ingenieros, coaches empresariales y muchos más profesionales de servicios verificados.' }
      ] %}
      {% set items = faqs|default(default_faqs) %}
      {% for faq in items %}
        <details class="pb-section__faq-item"
                 data-effect="fade-up"
                 data-delay="{{ loop.index0 * 100 }}">
          <summary>{{ faq.question|default('Pregunta')|t }}</summary>
          <div class="pb-section__faq-answer">
            {{ faq.answer|default('Respuesta.')|t }}
          </div>
        </details>
      {% endfor %}
    </div>
  </div>
</section>
