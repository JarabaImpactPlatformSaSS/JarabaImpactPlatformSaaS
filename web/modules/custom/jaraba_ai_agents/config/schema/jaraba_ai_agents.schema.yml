jaraba_ai_agents.brand_voice.*:
  type: config_object
  label: 'Brand Voice Configuration'
  mapping:
    archetype:
      type: string
      label: 'Brand Archetype'
    personality:
      type: mapping
      label: 'Personality Traits'
      mapping:
        formality:
          type: integer
          label: 'Formality (1-10)'
        warmth:
          type: integer
          label: 'Warmth (1-10)'
        confidence:
          type: integer
          label: 'Confidence (1-10)'
        humor:
          type: integer
          label: 'Humor (1-10)'
        technical:
          type: integer
          label: 'Technical Level (1-10)'
    examples:
      type: sequence
      label: 'Style Examples'
      sequence:
        type: mapping
        mapping:
          good:
            type: string
            label: 'Good Example'
          bad:
            type: string
            label: 'Bad Example'
          context:
            type: string
            label: 'Context/Situation'
    forbidden_terms:
      type: sequence
      label: 'Forbidden Terms'
      sequence:
        type: string
    preferred_terms:
      type: sequence
      label: 'Preferred Terms'
      sequence:
        type: string
    custom_instructions:
      type: string
      label: 'Custom Instructions'

# FIX-031: Provider fallback chain configuration schema.
jaraba_ai_agents.provider_fallback:
  type: config_object
  label: 'Provider Fallback Configuration'
  mapping:
    chains:
      type: mapping
      label: 'Fallback Chains per Tier'
      mapping:
        fast:
          type: sequence
          label: 'Fast Tier Chain'
          sequence:
            type: mapping
            mapping:
              provider_id:
                type: string
                label: 'Provider ID'
              model_id:
                type: string
                label: 'Model ID'
        balanced:
          type: sequence
          label: 'Balanced Tier Chain'
          sequence:
            type: mapping
            mapping:
              provider_id:
                type: string
                label: 'Provider ID'
              model_id:
                type: string
                label: 'Model ID'
        premium:
          type: sequence
          label: 'Premium Tier Chain'
          sequence:
            type: mapping
            mapping:
              provider_id:
                type: string
                label: 'Provider ID'
              model_id:
                type: string
                label: 'Model ID'

# FIX-020: Model routing tier configuration schema.
jaraba_ai_agents.model_routing:
  type: config_object
  label: 'Model Routing Configuration'
  mapping:
    tiers:
      type: mapping
      label: 'Model Tiers'
      mapping:
        fast:
          type: mapping
          label: 'Fast Tier'
          mapping:
            provider:
              type: string
              label: 'Provider ID'
            model:
              type: string
              label: 'Model ID'
            cost_per_1k_input:
              type: float
              label: 'Cost per 1K input tokens (USD)'
            cost_per_1k_output:
              type: float
              label: 'Cost per 1K output tokens (USD)'
            max_complexity:
              type: float
              label: 'Max complexity score (0.0-1.0)'
            use_cases:
              type: sequence
              label: 'Applicable use cases'
              sequence:
                type: string
        balanced:
          type: mapping
          label: 'Balanced Tier'
          mapping:
            provider:
              type: string
              label: 'Provider ID'
            model:
              type: string
              label: 'Model ID'
            cost_per_1k_input:
              type: float
              label: 'Cost per 1K input tokens (USD)'
            cost_per_1k_output:
              type: float
              label: 'Cost per 1K output tokens (USD)'
            max_complexity:
              type: float
              label: 'Max complexity score (0.0-1.0)'
            use_cases:
              type: sequence
              label: 'Applicable use cases'
              sequence:
                type: string
        premium:
          type: mapping
          label: 'Premium Tier'
          mapping:
            provider:
              type: string
              label: 'Provider ID'
            model:
              type: string
              label: 'Model ID'
            cost_per_1k_input:
              type: float
              label: 'Cost per 1K input tokens (USD)'
            cost_per_1k_output:
              type: float
              label: 'Cost per 1K output tokens (USD)'
            max_complexity:
              type: float
              label: 'Max complexity score (0.0-1.0)'
            use_cases:
              type: sequence
              label: 'Applicable use cases'
              sequence:
                type: string
