{
  "name": "jaraba-einvoice-b2b",
  "version": "1.0.0",
  "description": "SCSS del E-Invoice B2B - Facturacion electronica interempresarial",
  "private": true,
  "scripts": {
    "build": "sass scss/einvoice.scss:css/einvoice.css --style=compressed --no-source-map",
    "build:all": "npm run build",
    "watch": "sass --watch scss/einvoice.scss:css/einvoice.css --style=compressed"
  },
  "devDependencies": {
    "sass": "^1.71.0"
  }
}
