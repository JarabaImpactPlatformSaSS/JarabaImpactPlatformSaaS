<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
  <!-- Nodo raíz (círculo con relleno duotone) -->
  <circle cx="12" cy="4" r="2.5" fill="currentColor" fill-opacity="0.2" stroke="currentColor"/>
  <!-- Línea central -->
  <path d="M12 6.5v3" stroke="currentColor"/>
  <!-- Línea horizontal -->
  <path d="M6 11.5h12" stroke="currentColor"/>
  <!-- Líneas verticales a nodos hijos -->
  <path d="M6 11.5v3M12 11.5v3M18 11.5v3" stroke="currentColor"/>
  <!-- Nodos hijos (con relleno duotone) -->
  <circle cx="6" cy="17" r="2.5" fill="currentColor" fill-opacity="0.15" stroke="currentColor"/>
  <circle cx="12" cy="17" r="2.5" fill="currentColor" fill-opacity="0.25" stroke="currentColor"/>
  <circle cx="18" cy="17" r="2.5" fill="currentColor" fill-opacity="0.15" stroke="currentColor"/>
</svg>
