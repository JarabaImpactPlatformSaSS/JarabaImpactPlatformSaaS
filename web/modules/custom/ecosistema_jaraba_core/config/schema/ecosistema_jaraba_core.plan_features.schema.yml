# Schema for SaasPlanFeatures config entity
ecosistema_jaraba_core.plan_features.*:
  type: config_entity
  label: 'SaaS Plan Features configuration'
  mapping:
    id:
      type: string
      label: 'ID'
    label:
      type: label
      label: 'Label'
    uuid:
      type: string
      label: 'UUID'
    vertical:
      type: string
      label: 'Vertical machine name (_default for global fallback)'
    tier:
      type: string
      label: 'Tier machine name (starter, professional, enterprise)'
    features:
      type: sequence
      label: 'Enabled features'
      sequence:
        type: string
        label: 'Feature key'
    limits:
      type: sequence
      label: 'Numeric limits per resource (arbitrary key → integer)'
      sequence:
        type: integer
        label: 'Limit value'
    description:
      type: text
      label: 'Description'
    status:
      type: boolean
      label: 'Status'
