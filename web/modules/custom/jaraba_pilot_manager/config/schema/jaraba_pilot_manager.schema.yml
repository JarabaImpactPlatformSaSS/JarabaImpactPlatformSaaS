jaraba_pilot_manager.settings:
  type: config_object
  label: 'Pilot Manager settings'
  mapping:
    default_pilot_duration_days:
      type: integer
      label: 'Default pilot duration in days'
    max_tenants_per_pilot:
      type: integer
      label: 'Maximum tenants per pilot'
    auto_evaluate_on_complete:
      type: boolean
      label: 'Auto-evaluate on pilot completion'
