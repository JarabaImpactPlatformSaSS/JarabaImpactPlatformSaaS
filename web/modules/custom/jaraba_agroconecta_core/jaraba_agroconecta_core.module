<?php

/**
 * @file
 * Módulo AgroConecta Core.
 *
 * Marketplace agroalimentario multi-vendor con Commerce 3.x,
 * multi-tenancy y portales productor/cliente.
 *
 * Comando de compilación SCSS:
 *   cd web/modules/custom/jaraba_agroconecta_core && npm run build
 */

// AUDIT-CONS-N05: All route name references normalized from jaraba_agroconecta.* to jaraba_agroconecta_core.*
declare(strict_types=1);

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function jaraba_agroconecta_core_help(string $route_name, RouteMatchInterface $route_match): string
{
    if ($route_name === 'help.page.jaraba_agroconecta_core') {
        return '<p>' . t('Módulo AgroConecta: Marketplace agroalimentario multi-vendor con gestión de productos, productores y certificaciones.') . '</p>';
    }
    return '';
}

/**
 * Implements hook_theme().
 *
 * Define los templates Twig del módulo AgroConecta.
 */
function jaraba_agroconecta_core_theme(): array
{
    return [
        'agro_marketplace' => [
            'variables' => [
                'products' => [],
                'marketplace_name' => '',
                'marketplace_description' => '',
                'total_products' => 0,
                'pager' => NULL,
            ],
            'template' => 'agro-marketplace',
        ],
        'agro_product_detail' => [
            'variables' => [
                'product' => NULL,
                'producer' => NULL,
                'whatsapp_enabled' => FALSE,
                'whatsapp_phone' => '',
                'whatsapp_message' => '',
            ],
            'template' => 'agro-product-detail',
        ],
        'agro_checkout' => [
            'variables' => [
                'marketplace_name' => '',
            ],
            'template' => 'agro-checkout',
        ],
        'agro_order_confirmation' => [
            'variables' => [
                'order' => [],
                'order_number' => '',
            ],
            'template' => 'agro-order-confirmation',
        ],
        // ===== Portal del Productor (Fase 3) =====
        'agro_producer_dashboard' => [
            'variables' => [
                'kpis' => [],
                'alerts' => [],
                'pending_orders' => [],
                'top_products' => [],
                'setup_wizard' => NULL,
                'daily_actions' => [],
            ],
            'template' => 'agro-producer-dashboard',
        ],
        'agro_producer_orders' => [
            'variables' => [
                'orders' => [],
                'total' => 0,
            ],
            'template' => 'agro-producer-orders',
        ],
        'agro_producer_order_detail' => [
            'variables' => [
                'suborder' => [],
                'order' => [],
                'items' => [],
            ],
            'template' => 'agro-producer-order-detail',
        ],
        'agro_producer_products' => [
            'variables' => [
                'products' => [],
                'total' => 0,
            ],
            'template' => 'agro-producer-products',
        ],
        'agro_producer_payouts' => [
            'variables' => [
                'total_revenue' => '',
                'total_commissions' => '',
                'total_payouts' => '',
                'payout_history' => [],
            ],
            'template' => 'agro-producer-payouts',
        ],
        'agro_producer_settings' => [
            'variables' => [
                'producer' => [],
            ],
            'template' => 'agro-producer-settings',
        ],
        // ===== Portal del Cliente (Fase 3) =====
        'agro_customer_dashboard' => [
            'variables' => [
                'active_order' => NULL,
                'total_orders' => 0,
                'recent_products' => [],
                'user_name' => '',
            ],
            'template' => 'agro-customer-dashboard',
        ],
        'agro_customer_orders' => [
            'variables' => [
                'orders' => [],
                'total' => 0,
            ],
            'template' => 'agro-customer-orders',
        ],
        'agro_customer_order_detail' => [
            'variables' => [
                'order' => [],
                'items' => [],
                'timeline' => [],
            ],
            'template' => 'agro-customer-order-detail',
        ],
        'agro_customer_addresses' => [
            'variables' => [
                'addresses' => [],
            ],
            'template' => 'agro-customer-addresses',
        ],
        'agro_customer_favorites' => [
            'variables' => [
                'products' => [],
            ],
            'template' => 'agro-customer-favorites',
        ],
        // ===== Search & Discovery (Fase 5) =====
        'agro_search_page' => [
            'variables' => [
                'search_query' => '',
                'sort' => 'relevance',
                'categories' => [],
                'featured_collections' => [],
                'initial_results' => NULL,
            ],
            'template' => 'agro-search-page',
        ],
        'agro_category_page' => [
            'variables' => [
                'category' => [],
                'products' => [],
            ],
            'template' => 'agro-category-page',
        ],
        'agro_collection_page' => [
            'variables' => [
                'collection' => [],
                'products' => [],
                'total' => 0,
            ],
            'template' => 'agro-collection-page',
        ],
        'agro_traceability_public' => [
            'variables' => [
                'batch' => NULL,
                'producer' => NULL,
                'product' => NULL,
                'events' => [],
            ],
            'template' => 'agro-traceability-public',
        ],
        // Caso de éxito: Cooperativa Sierra de Cazorla.
        'agroconecta_case_study' => [
            'variables' => [
                'hero_image' => '',
                'antonio_image' => '',
                'before_after_image' => '',
                'qr_image' => '',
                'dashboard_image' => '',
                'chef_image' => '',
                'metrics' => [],
                'testimonial' => [],
                'timeline' => [],
                'pricing_url' => '',
                'register_url' => '',
            ],
            'template' => 'agroconecta-case-study',
            'path' => \Drupal::service('extension.list.theme')->getPath('ecosistema_jaraba_theme') . '/templates',
        ],
    ];
}

/**
 * Implements hook_preprocess_html().
 *
 * CRÍTICO: Añade body classes para las páginas AgroConecta.
 * NUNCA usar attributes.addClass() en templates Twig.
 *
 * Plan Elevación AgroConecta Clase Mundial v1 — Fase 3 (Body Classes Consolidation).
 * Todas las rutas del vertical reciben:
 *   - Su clase específica de ruta.
 *   - 'agro-page' como clase common del vertical.
 *   - 'page--clean-layout' para indicar layout zero-region.
 *
 * @see /frontend-page-pattern workflow
 */
function jaraba_agroconecta_core_preprocess_html(array &$variables): void
{
    $route = \Drupal::routeMatch()->getRouteName();
    if ($route === NULL) {
        return;
    }

    $routeClasses = [
        // Marketplace público
        'jaraba_agroconecta_core.marketplace' => ['marketplace-page', 'page-marketplace'],
        'jaraba_agroconecta_core.product_detail' => ['agro-product-page'],
        'jaraba_agroconecta_core.checkout' => ['checkout-page', 'page-checkout'],
        'jaraba_agroconecta_core.order_confirmation' => ['order-confirmation-page'],
        // Portal del Productor (Fase 3)
        'jaraba_agroconecta_core.producer.dashboard' => ['producer-dashboard-page', 'producer-portal-page'],
        'jaraba_agroconecta_core.producer.orders' => ['producer-orders-page', 'producer-portal-page'],
        'jaraba_agroconecta_core.producer.order_detail' => ['producer-order-detail-page', 'producer-portal-page'],
        'jaraba_agroconecta_core.producer.products' => ['producer-products-page', 'producer-portal-page'],
        'jaraba_agroconecta_core.producer.payouts' => ['producer-payouts-page', 'producer-portal-page'],
        'jaraba_agroconecta_core.producer.settings' => ['producer-settings-page', 'producer-portal-page'],
        // Portal del Cliente (Fase 3)
        'jaraba_agroconecta_core.customer.dashboard' => ['customer-dashboard-page', 'customer-portal-page'],
        'jaraba_agroconecta_core.customer.orders' => ['customer-orders-page', 'customer-portal-page'],
        'jaraba_agroconecta_core.customer.order_detail' => ['customer-order-detail-page', 'customer-portal-page'],
        'jaraba_agroconecta_core.customer.addresses' => ['customer-addresses-page', 'customer-portal-page'],
        'jaraba_agroconecta_core.customer.favorites' => ['customer-favorites-page', 'customer-portal-page'],
        // Search & Discovery (Fase 5)
        'jaraba_agroconecta_core.search_page' => ['search-page', 'agro-search-page'],
        'jaraba_agroconecta_core.category_page' => ['category-page', 'agro-category-page'],
        'jaraba_agroconecta_core.collection_page' => ['collection-page', 'agro-collection-page'],
        // Trazabilidad Pública (Fase 6)
        'jaraba_agroconecta.traceability.public' => ['traceability-public-page', 'agro-traceability-page'],
        // Landing page del vertical
        'ecosistema_jaraba_core.landing.agroconecta' => ['agroconecta-landing-page'],
    ];

    if (isset($routeClasses[$route])) {
        foreach ($routeClasses[$route] as $class) {
            $variables['attributes']['class'][] = $class;
        }
        // Clases comunes del vertical: identificador + layout zero-region.
        $variables['attributes']['class'][] = 'agro-page';
        $variables['attributes']['class'][] = 'page--clean-layout';
    }
}

/**
 * Implements hook_entity_insert().
 *
 * Triggers automáticos del Producer Copilot:
 * - Nuevo producto sin descripción → Encola generación con Copilot.
 * - Nueva reseña recibida → Encola borrador de respuesta.
 */
function jaraba_agroconecta_core_entity_insert(\Drupal\Core\Entity\EntityInterface $entity): void
{
    // Trigger: Producto sin descripción → generar con Copilot.
    if ($entity->getEntityTypeId() === 'product_agro') {
        $description = '';
        try {
            if ($entity->hasField('body')) {
                $description = trim((string) ($entity->get('body')->value ?? ''));
            }
        }
        catch (\Exception $e) {
            // Campo no disponible.
        }

        if (empty($description)) {
            \Drupal::queue('jaraba_agroconecta_copilot_auto')
                ->createItem([
                    'action' => 'generate_description',
                    'product_id' => (int) $entity->id(),
                    'tenant_id' => $entity->hasField('tenant_id') ? ($entity->get('tenant_id')->target_id ?? NULL) : NULL,
                ]);

            \Drupal::logger('jaraba_agroconecta_core')->info(
                'Producto @id creado sin descripción: encolado para generación automática.',
                ['@id' => $entity->id()]
            );
        }
    }

    // Trigger: Nueva reseña → generar borrador de respuesta.
    if ($entity->getEntityTypeId() === 'review_agro') {
        $rating = 3;
        try {
            $rating = (int) ($entity->get('rating')->value ?? 3);
        }
        catch (\Exception $e) {
            // Campo no disponible.
        }

        \Drupal::queue('jaraba_agroconecta_copilot_auto')
            ->createItem([
                'action' => 'respond_review',
                'review_id' => (int) $entity->id(),
                'review_rating' => $rating,
                'tenant_id' => $entity->hasField('tenant_id') ? ($entity->get('tenant_id')->target_id ?? NULL) : NULL,
            ]);

        \Drupal::logger('jaraba_agroconecta_core')->info(
            'Reseña @id creada: encolado borrador de respuesta automática.',
            ['@id' => $entity->id()]
        );
    }
}

/**
 * Implements hook_cron().
 *
 * Tareas periódicas del módulo AgroConecta:
 * - Actualizar forecasts de demanda (cada 24h, 04:00-06:00 UTC).
 * - Detectar precios fuera de rango y generar alertas (cada 12h).
 */
function jaraba_agroconecta_core_cron(): void
{
    $state = \Drupal::state();
    $time = \Drupal::time()->getRequestTime();

    // === Actualización de forecasts de demanda (cada 24h) ===
    $lastForecast = (int) $state->get('jaraba_agroconecta_core.last_forecast_update', 0);
    $hour = (int) date('G', $time);

    if (($time - $lastForecast) > 86400 && $hour >= 4 && $hour <= 6) {
        $state->set('jaraba_agroconecta_core.last_forecast_update', $time);

        // AUDIT-PERF-N11: Queue per-producer forecast instead of synchronous loop.
        $storage = \Drupal::entityTypeManager()->getStorage('producer_profile');
        $producerIds = $storage->getQuery()
            ->accessCheck(FALSE)
            ->condition('status', 1)
            ->execute();

        $queue = \Drupal::queue('jaraba_agroconecta_producer_forecast');
        foreach ($producerIds as $producerId) {
            $queue->createItem([
                'producer_id' => (int) $producerId,
                'operation' => 'forecast',
            ]);
        }

        \Drupal::logger('jaraba_agroconecta_core')->info(
            'Cron: @count productores encolados para forecast.',
            ['@count' => count($producerIds)]
        );
    }

    // === Alertas de precios fuera de rango (cada 12h) ===
    $lastPriceCheck = (int) $state->get('jaraba_agroconecta_core.last_price_check', 0);

    if (($time - $lastPriceCheck) > 43200) {
        $state->set('jaraba_agroconecta_core.last_price_check', $time);

        // AUDIT-PERF-N11: Queue per-producer price alerts instead of synchronous loop.
        $storage = \Drupal::entityTypeManager()->getStorage('producer_profile');
        $producerIds = $storage->getQuery()
            ->accessCheck(FALSE)
            ->condition('status', 1)
            ->execute();

        $queue = \Drupal::queue('jaraba_agroconecta_producer_forecast');
        foreach ($producerIds as $producerId) {
            $queue->createItem([
                'producer_id' => (int) $producerId,
                'operation' => 'price_alerts',
            ]);
        }

        \Drupal::logger('jaraba_agroconecta_core')->info(
            'Cron: @count productores encolados para alertas de precio.',
            ['@count' => count($producerIds)]
        );
    }

    // === Sincronización catálogo WhatsApp Commerce (cada 6h) ===
    $lastWaSync = (int) $state->get('jaraba_agroconecta_core.whatsapp_catalog_last_sync', 0);
    if (($time - $lastWaSync) > 21600 && \Drupal::hasService('jaraba_agroconecta_core.whatsapp_catalog_sync')) {
        $state->set('jaraba_agroconecta_core.whatsapp_catalog_last_sync', $time);
        try {
            $catalogId = getenv('WHATSAPP_CATALOG_ID');
            if ($catalogId) {
                \Drupal::service('jaraba_agroconecta_core.whatsapp_catalog_sync')->syncProducts($catalogId, 0);
            }
        }
        catch (\Throwable $e) {
            \Drupal::logger('jaraba_agroconecta_core')->error('WhatsApp catalog sync cron error: @msg', ['@msg' => $e->getMessage()]);
        }
    }

    // === Detección de carritos abandonados (cada 1h) ===
    $lastCartCheck = (int) $state->get('jaraba_agroconecta_core.last_cart_recovery_check', 0);

    if (($time - $lastCartCheck) > 3600) {
        $state->set('jaraba_agroconecta_core.last_cart_recovery_check', $time);

        try {
            /** @var \Drupal\jaraba_agroconecta_core\Service\CartRecoveryService $cartRecovery */
            $cartRecovery = \Drupal::service('jaraba_agroconecta_core.cart_recovery');
            $detected = $cartRecovery->detectAbandonedCarts();

            if ($detected > 0) {
                \Drupal::logger('jaraba_agroconecta_core')->info(
                    'Cron: @count carritos abandonados detectados para recuperación.',
                    ['@count' => $detected]
                );
            }
        }
        catch (\Exception $e) {
            \Drupal::logger('jaraba_agroconecta_core')->error(
                'Cron: error en detección de carritos abandonados: @error',
                ['@error' => $e->getMessage()]
            );
        }
    }
}

/**
 * Implements hook_mail().
 *
 * Plantillas de email del módulo AgroConecta.
 */
function jaraba_agroconecta_core_mail(string $key, array &$message, array $params): void
{
    switch ($key) {
        case 'copilot_content_ready':
            $message['subject'] = t('Tu contenido IA está listo para revisión');
            $message['body'][] = t('Hola @name,', ['@name' => $params['producer_name'] ?? 'Productor']);
            $message['body'][] = t('El copiloto ha generado nuevo contenido para tu producto "@product". Revísalo y apruébalo desde tu panel de control.', [
                '@product' => $params['product_name'] ?? '',
            ]);
            break;

        case 'price_alert':
            $message['subject'] = t('Alerta de precio: @product', ['@product' => $params['product_name'] ?? '']);
            $message['body'][] = t('Hola @name,', ['@name' => $params['producer_name'] ?? 'Productor']);
            $message['body'][] = t('Tu producto "@product" tiene un precio @deviation del promedio del mercado (@avg€). @recommendation', [
                '@product' => $params['product_name'] ?? '',
                '@deviation' => $params['deviation'] ?? '',
                '@avg' => $params['market_avg'] ?? '',
                '@recommendation' => $params['recommendation'] ?? '',
            ]);
            break;

        case 'demand_forecast':
            $message['subject'] = t('Previsión de demanda: @product', ['@product' => $params['product_name'] ?? '']);
            $message['body'][] = t('Hola @name,', ['@name' => $params['producer_name'] ?? 'Productor']);
            $message['body'][] = t('Basándonos en las tendencias de ventas, la demanda de "@product" se prevé @trend en los próximos 30 días.', [
                '@product' => $params['product_name'] ?? '',
                '@trend' => $params['trend'] ?? 'estable',
            ]);
            break;

        case 'cart_recovery':
            $message['subject'] = t('¡No te olvides de tu carrito! @incentive', [
                '@incentive' => !empty($params['discount_percent']) ? '— ' . $params['discount_percent'] . '% de descuento' : '',
            ]);
            $message['body'][] = t('Hola @name,', ['@name' => $params['customer_name'] ?? 'Cliente']);
            $message['body'][] = $params['message'] ?? t('Tus productos artesanales favoritos siguen esperándote en tu carrito.');
            if (!empty($params['discount_code'])) {
                $message['body'][] = t('Usa el código @code para obtener tu descuento.', ['@code' => $params['discount_code']]);
            }
            break;
    }
}

/**
 * Implements hook_page_attachments_alter().
 *
 * Adds Open Graph meta tags for AgroConecta public pages.
 */
function jaraba_agroconecta_core_page_attachments_alter(array &$attachments): void
{
    $route = \Drupal::routeMatch()->getRouteName();
    if ($route === NULL) {
        return;
    }

    $ogRoutes = [
        'jaraba_agroconecta_core.marketplace' => [
            'title' => t('Marketplace AgroConecta — Productos frescos y locales'),
            'description' => t('Descubre productos agroalimentarios de productores locales. Del campo a tu mesa, sin intermediarios.'),
            'type' => 'website',
        ],
        'jaraba_agroconecta_core.product_detail' => [
            'title' => t('Producto — AgroConecta'),
            'description' => t('Producto fresco de productor local verificado en AgroConecta.'),
            'type' => 'product',
        ],
        'jaraba_agroconecta_core.category_page' => [
            'title' => t('Categoría — AgroConecta'),
            'description' => t('Explora productos por categoría en el marketplace AgroConecta.'),
            'type' => 'website',
        ],
        'jaraba_agroconecta_core.search_page' => [
            'title' => t('Buscar productos — AgroConecta'),
            'description' => t('Busca y descubre productos agroalimentarios frescos de tu zona.'),
            'type' => 'website',
        ],
    ];

    if (!isset($ogRoutes[$route])) {
        return;
    }

    $og = $ogRoutes[$route];
    $siteUrl = \Drupal::request()->getSchemeAndHttpHost();

    $meta = [
        ['property' => 'og:title', 'content' => (string) $og['title']],
        ['property' => 'og:description', 'content' => (string) $og['description']],
        ['property' => 'og:type', 'content' => $og['type']],
        ['property' => 'og:url', 'content' => $siteUrl . \Drupal::request()->getRequestUri()],
        ['property' => 'og:site_name', 'content' => \Drupal::config('system.site')->get('name')],
    ];

    foreach ($meta as $tag) {
        $attachments['#attached']['html_head'][] = [
            [
                '#tag' => 'meta',
                '#attributes' => $tag,
            ],
            'og_' . str_replace(':', '_', $tag['property']),
        ];
    }

    // Schema.org Product JSON-LD for product detail pages (GAP-SCHEMA-8).
    if ($route === 'jaraba_agroconecta_core.product_detail') {
        $routeMatch = \Drupal::routeMatch();
        $product = $routeMatch->getParameter('product_agro');
        if ($product) {
            $schema = [
                '@context' => 'https://schema.org',
                '@type' => 'Product',
                'name' => $product->label() ?? '',
                'url' => $siteUrl . \Drupal::request()->getRequestUri(),
            ];

            // Description.
            if ($product->hasField('description_short') && !$product->get('description_short')->isEmpty()) {
                $schema['description'] = $product->get('description_short')->value;
            }
            elseif ($product->hasField('description') && !$product->get('description')->isEmpty()) {
                $schema['description'] = mb_substr(strip_tags($product->get('description')->value), 0, 300);
            }

            // SKU.
            if ($product->hasField('sku') && !$product->get('sku')->isEmpty()) {
                $schema['sku'] = $product->get('sku')->value;
            }

            // Category.
            if ($product->hasField('category') && !$product->get('category')->isEmpty()) {
                $schema['category'] = $product->get('category')->value;
            }

            // Image.
            if ($product->hasField('image') && !$product->get('image')->isEmpty()) {
                $file = $product->get('image')->entity;
                if ($file) {
                    $schema['image'] = \Drupal::service('file_url_generator')
                        ->generateAbsoluteString($file->getFileUri());
                }
            }

            // Producer as Brand.
            if ($product->hasField('producer_id') && !$product->get('producer_id')->isEmpty()) {
                $producer = $product->get('producer_id')->entity;
                if ($producer) {
                    $schema['brand'] = [
                        '@type' => 'Brand',
                        'name' => $producer->label() ?? '',
                    ];
                }
            }

            // Origin.
            if ($product->hasField('origin') && !$product->get('origin')->isEmpty()) {
                $schema['countryOfOrigin'] = [
                    '@type' => 'Country',
                    'name' => $product->get('origin')->value,
                ];
            }

            // Offers (price + availability).
            $offers = [
                '@type' => 'Offer',
                'url' => $schema['url'],
                'priceCurrency' => 'EUR',
            ];

            if ($product->hasField('price') && !$product->get('price')->isEmpty()) {
                $offers['price'] = (string) $product->get('price')->value;
            }

            if ($product->hasField('currency_code') && !$product->get('currency_code')->isEmpty()) {
                $offers['priceCurrency'] = $product->get('currency_code')->value;
            }

            // Availability based on stock.
            if ($product->hasField('stock')) {
                $stock = (int) ($product->get('stock')->value ?? 0);
                $offers['availability'] = $stock > 0
                    ? 'https://schema.org/InStock'
                    : 'https://schema.org/OutOfStock';
            }

            $schema['offers'] = $offers;

            // AggregateRating from reviews.
            try {
                if (\Drupal::hasService('ecosistema_jaraba_core.review_schema_org')) {
                    $reviewSchemaService = \Drupal::service('ecosistema_jaraba_core.review_schema_org');
                    $aggregateRating = $reviewSchemaService->generateAggregateRating('review_agro', [
                        'product_id' => $product->id(),
                    ]);
                    if (!empty($aggregateRating)) {
                        $schema['aggregateRating'] = $aggregateRating;
                    }
                }
            }
            catch (\Exception $e) {
                // Graceful degradation: skip reviews in schema.
            }

            $attachments['#attached']['html_head'][] = [
                [
                    '#type' => 'html_tag',
                    '#tag' => 'script',
                    '#attributes' => ['type' => 'application/ld+json'],
                    '#value' => json_encode($schema, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE),
                ],
                'schema_org_product',
            ];
        }
    }
}

/**
 * Implements hook_preprocess_page().
 *
 * Inyecta variables de página para el zero-region pattern.
 * Plan Elevación AgroConecta Clase Mundial v1 — Fase 4
 * Directriz ZERO-REGION-001.
 */
function jaraba_agroconecta_core_preprocess_page(array &$variables): void
{
    $route_name = \Drupal::routeMatch()->getRouteName() ?? '';

    // Solo procesar rutas del vertical AgroConecta (excluir case studies).
    if ((str_starts_with($route_name, 'jaraba_agroconecta_core.') ||
        str_starts_with($route_name, 'jaraba_agroconecta.') ||
        $route_name === 'ecosistema_jaraba_core.landing.agroconecta') && !str_contains($route_name, '.case_study.')) {
        // Inyectar clean_content desde el controller render array.
        if (isset($variables['page']['content'])) {
            $variables['clean_content'] = $variables['page']['content'];
        }

        // Contexto del vertical para templates.
        $variables['agroconecta_context'] = [
            'vertical' => 'agroconecta',
            'route' => $route_name,
        ];

        // Copilot FAB habilitado para rutas con usuario autenticado.
        $variables['copilot_enabled'] = \Drupal::currentUser()->isAuthenticated();
    }
}

/**
 * Implements hook_theme_suggestions_page_alter().
 *
 * Redirige todas las rutas frontend de AgroConecta al template
 * page--agroconecta.html.twig (zero-region).
 * Plan Elevación AgroConecta Clase Mundial v1 — Fase 4
 * Directriz ZERO-REGION-001.
 */
function jaraba_agroconecta_core_theme_suggestions_page_alter(array &$suggestions, array $variables): void
{
    $route_name = \Drupal::routeMatch()->getRouteName() ?? '';

    // Rutas frontend del vertical (excluir rutas API).
    if (((str_starts_with($route_name, 'jaraba_agroconecta_core.') || str_starts_with($route_name, 'jaraba_agroconecta.')) && !str_contains($route_name, '.api.') && !str_contains($route_name, '.case_study.')) ||
        $route_name === 'ecosistema_jaraba_core.landing.agroconecta') {
        $suggestions[] = 'page__agroconecta';
    }
}


/**
 * Prepares variables for agro_batch templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_batch(array &$variables): void {
  $entity = $variables['elements']['#agro_batch'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_batch'] = $entity;
}

/**
 * Prepares variables for agro_category templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_category(array &$variables): void {
  $entity = $variables['elements']['#agro_category'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_category'] = $entity;
}

/**
 * Prepares variables for agro_certification templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_certification(array &$variables): void {
  $entity = $variables['elements']['#agro_certification'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_certification'] = $entity;
}

/**
 * Prepares variables for agro_collection templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_collection(array &$variables): void {
  $entity = $variables['elements']['#agro_collection'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_collection'] = $entity;
}

/**
 * Prepares variables for agro_shipment templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_shipment(array &$variables): void {
  $entity = $variables['elements']['#agro_shipment'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_shipment'] = $entity;
}

/**
 * Prepares variables for agro_shipping_rate templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_shipping_rate(array &$variables): void {
  $entity = $variables['elements']['#agro_shipping_rate'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_shipping_rate'] = $entity;
}

/**
 * Prepares variables for agro_shipping_zone templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_shipping_zone(array &$variables): void {
  $entity = $variables['elements']['#agro_shipping_zone'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_shipping_zone'] = $entity;
}

/**
 * Prepares variables for alert_rule_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_alert_rule_agro(array &$variables): void {
  $entity = $variables['elements']['#alert_rule_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['alert_rule_agro'] = $entity;
}

/**
 * Prepares variables for analytics_daily_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_analytics_daily_agro(array &$variables): void {
  $entity = $variables['elements']['#analytics_daily_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['analytics_daily_agro'] = $entity;
}

/**
 * Prepares variables for agro_carrier_config templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_agro_carrier_config(array &$variables): void {
  $entity = $variables['elements']['#agro_carrier_config'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['agro_carrier_config'] = $entity;
}

/**
 * Prepares variables for copilot_conversation_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_copilot_conversation_agro(array &$variables): void {
  $entity = $variables['elements']['#copilot_conversation_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['copilot_conversation_agro'] = $entity;
}

/**
 * Prepares variables for copilot_message_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_copilot_message_agro(array &$variables): void {
  $entity = $variables['elements']['#copilot_message_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['copilot_message_agro'] = $entity;
}

/**
 * Prepares variables for coupon_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_coupon_agro(array &$variables): void {
  $entity = $variables['elements']['#coupon_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['coupon_agro'] = $entity;
}

/**
 * Prepares variables for customer_preference_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_customer_preference_agro(array &$variables): void {
  $entity = $variables['elements']['#customer_preference_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['customer_preference_agro'] = $entity;
}

/**
 * Prepares variables for document_download_log templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_document_download_log(array &$variables): void {
  $entity = $variables['elements']['#document_download_log'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['document_download_log'] = $entity;
}

/**
 * Prepares variables for integrity_proof_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_integrity_proof_agro(array &$variables): void {
  $entity = $variables['elements']['#integrity_proof_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['integrity_proof_agro'] = $entity;
}

/**
 * Prepares variables for notification_log_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_notification_log_agro(array &$variables): void {
  $entity = $variables['elements']['#notification_log_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['notification_log_agro'] = $entity;
}

/**
 * Prepares variables for notification_preference_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_notification_preference_agro(array &$variables): void {
  $entity = $variables['elements']['#notification_preference_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['notification_preference_agro'] = $entity;
}

/**
 * Prepares variables for notification_template_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_notification_template_agro(array &$variables): void {
  $entity = $variables['elements']['#notification_template_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['notification_template_agro'] = $entity;
}

/**
 * Prepares variables for order_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_order_agro(array &$variables): void {
  $entity = $variables['elements']['#order_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['order_agro'] = $entity;
}

/**
 * Prepares variables for order_item_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_order_item_agro(array &$variables): void {
  $entity = $variables['elements']['#order_item_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['order_item_agro'] = $entity;
}

/**
 * Prepares variables for partner_relationship templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_partner_relationship(array &$variables): void {
  $entity = $variables['elements']['#partner_relationship'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['partner_relationship'] = $entity;
}

/**
 * Prepares variables for producer_profile templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_producer_profile(array &$variables): void {
  $entity = $variables['elements']['#producer_profile'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['producer_profile'] = $entity;
}

/**
 * Prepares variables for product_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_product_agro(array &$variables): void {
  $entity = $variables['elements']['#product_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['product_agro'] = $entity;
}

/**
 * Prepares variables for product_document templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_product_document(array &$variables): void {
  $entity = $variables['elements']['#product_document'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['product_document'] = $entity;
}

/**
 * Prepares variables for promotion_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_promotion_agro(array &$variables): void {
  $entity = $variables['elements']['#promotion_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['promotion_agro'] = $entity;
}

/**
 * Prepares variables for qr_code_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_qr_code_agro(array &$variables): void {
  $entity = $variables['elements']['#qr_code_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['qr_code_agro'] = $entity;
}

/**
 * Prepares variables for qr_lead_capture templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_qr_lead_capture(array &$variables): void {
  $entity = $variables['elements']['#qr_lead_capture'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['qr_lead_capture'] = $entity;
}

/**
 * Prepares variables for qr_scan_event templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_qr_scan_event(array &$variables): void {
  $entity = $variables['elements']['#qr_scan_event'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['qr_scan_event'] = $entity;
}

/**
 * Prepares variables for review_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_review_agro(array &$variables): void {
  $entity = $variables['elements']['#review_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['review_agro'] = $entity;
}

/**
 * Prepares variables for sales_conversation_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_sales_conversation_agro(array &$variables): void {
  $entity = $variables['elements']['#sales_conversation_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['sales_conversation_agro'] = $entity;
}

/**
 * Prepares variables for sales_message_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_sales_message_agro(array &$variables): void {
  $entity = $variables['elements']['#sales_message_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['sales_message_agro'] = $entity;
}

/**
 * Prepares variables for shipping_method_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_shipping_method_agro(array &$variables): void {
  $entity = $variables['elements']['#shipping_method_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['shipping_method_agro'] = $entity;
}

/**
 * Prepares variables for shipping_zone_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_shipping_zone_agro(array &$variables): void {
  $entity = $variables['elements']['#shipping_zone_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['shipping_zone_agro'] = $entity;
}

/**
 * Prepares variables for suborder_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_suborder_agro(array &$variables): void {
  $entity = $variables['elements']['#suborder_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['suborder_agro'] = $entity;
}

/**
 * Prepares variables for trace_event_agro templates.
 *
 * @param array $variables
 *   An associative array containing entity data.
 */
function jaraba_agroconecta_core_preprocess_trace_event_agro(array &$variables): void {
  $entity = $variables['elements']['#trace_event_agro'] ?? NULL;
  if ($entity === NULL) {
    return;
  }
  $variables['trace_event_agro'] = $entity;
}

