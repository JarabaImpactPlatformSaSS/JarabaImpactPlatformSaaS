# ==============================================
# DEPLOY A PRODUCCIÓN (IONOS)
# ==============================================
# Pipeline PRINCIPAL de deploy a producción.
# Host: plataformadeecosistemas.com (IONOS shared hosting)
# Estrategia: git-based deploy con maintenance mode.
#
# Flujo: Lint → Test → Backup → Deploy → Smoke Test
#
# Para el pipeline Blue-Green containerizado (futuro),
# ver: deploy-production.yml
# ==============================================

name: Jaraba SaaS - Deploy to IONOS

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_backup:
        description: 'Skip pre-deployment backup'
        required: false
        default: 'false'
        type: boolean

env:
  PHP_VERSION: '8.4'
  IONOS_HOST: 'access834313033.webspace-data.io'
  IONOS_USER: 'u101456434'
  SITE_DIR: '~/JarabaImpactPlatformSaaS'

jobs:
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, xml, ctype, iconv, intl, pdo_mysql, pdo_sqlite, sqlite3, dom, filter, gd, json, phar
          coverage: none

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Run PHPUnit Unit tests
        run: vendor/bin/phpunit --configuration phpunit.xml --no-progress --testsuite Unit

      - name: Run PHPUnit Kernel tests
        env:
          SIMPLETEST_DB: sqlite://localhost//tmp/test.sqlite
          SIMPLETEST_BASE_URL: http://localhost
        run: vendor/bin/phpunit --configuration phpunit.xml --no-progress --testsuite Kernel

  deploy:
    name: 🚀 Deploy to IONOS
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.IONOS_SSH_PRIVATE_KEY }}

      - name: Add IONOS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ env.IONOS_HOST }} >> ~/.ssh/known_hosts

      - name: Create backup (unless skipped)
        if: ${{ github.event.inputs.skip_backup != 'true' }}
        continue-on-error: true
        run: |
          ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} << 'EOF'
            cd ~/JarabaImpactPlatformSaaS
            mkdir -p ~/backups
            /usr/bin/php8.4-cli vendor/bin/drush.php sql-dump --gzip > ~/backups/db_pre_deploy_$(date +%Y%m%d_%H%M%S).sql.gz || {
              echo "⚠️ Drush sql-dump falló. Intentando mysqldump directo..."
              # Fallback: mysqldump directo sin bootstrap de Drupal
              # Buscar credenciales en settings.local.php (producción) primero, luego settings.php
              SETTINGS_FILE="web/sites/default/settings.local.php"
              if [ ! -f "$SETTINGS_FILE" ]; then
                SETTINGS_FILE="web/sites/default/settings.php"
              fi
              DBNAME=$(grep "'database'" "$SETTINGS_FILE" | head -1 | grep -o "'[^']*'" | tail -1 | tr -d "'")
              DBUSER=$(grep "'username'" "$SETTINGS_FILE" | head -1 | grep -o "'[^']*'" | tail -1 | tr -d "'")
              DBPASS=$(grep "'password'" "$SETTINGS_FILE" | head -1 | grep -o "'[^']*'" | tail -1 | tr -d "'")
              if [ -n "$DBNAME" ] && [ -n "$DBUSER" ]; then
                mysqldump -u"$DBUSER" -p"$DBPASS" "$DBNAME" | gzip > ~/backups/db_pre_deploy_$(date +%Y%m%d_%H%M%S).sql.gz
                echo "✅ Backup realizado con mysqldump directo"
              else
                echo "⚠️ No se pudo extraer credenciales BD. Continuando sin backup."
              fi
            }
          EOF

      - name: Enable maintenance mode
        continue-on-error: true
        run: |
          ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} << 'EOF'
            cd ~/JarabaImpactPlatformSaaS
            /usr/bin/php8.4-cli vendor/bin/drush.php state:set system.maintenance_mode 1 || echo "⚠️ No se pudo activar maintenance mode. Continuando deploy."
          EOF

      - name: Ensure settings.local.php exists
        run: |
          EXISTS=$(ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} "test -f ~/JarabaImpactPlatformSaaS/web/sites/default/settings.local.php && echo YES || echo NO")
          if [ "$EXISTS" = "YES" ]; then
            echo "✅ settings.local.php ya existe en IONOS"
          elif [ -n "${{ secrets.IONOS_DB_HOST }}" ]; then
            echo "⚠️ Creando settings.local.php desde GitHub secrets..."
            printf '<?php\n// Production DB credentials (auto-generated by CI/CD).\n' > /tmp/settings.local.php
            printf "\$databases['default']['default'] = [\n" >> /tmp/settings.local.php
            printf "  'database' => '%s',\n" '${{ secrets.IONOS_DB_NAME }}' >> /tmp/settings.local.php
            printf "  'username' => '%s',\n" '${{ secrets.IONOS_DB_USER }}' >> /tmp/settings.local.php
            printf "  'password' => '%s',\n" '${{ secrets.IONOS_DB_PASS }}' >> /tmp/settings.local.php
            printf "  'host' => '%s',\n" '${{ secrets.IONOS_DB_HOST }}' >> /tmp/settings.local.php
            printf "  'prefix' => '',\n  'port' => 3306,\n" >> /tmp/settings.local.php
            printf "  'isolation_level' => 'READ COMMITTED',\n" >> /tmp/settings.local.php
            printf "  'driver' => 'mysql',\n" >> /tmp/settings.local.php
            printf "  'namespace' => 'Drupal\\\\\\\\mysql\\\\\\\\Driver\\\\\\\\Database\\\\\\\\mysql',\n" >> /tmp/settings.local.php
            printf "  'autoload' => 'core/modules/mysql/src/Driver/Database/mysql/',\n];\n" >> /tmp/settings.local.php
            ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} "chmod 755 ~/JarabaImpactPlatformSaaS/web/sites/default 2>/dev/null || true"
            scp /tmp/settings.local.php ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }}:~/JarabaImpactPlatformSaaS/web/sites/default/settings.local.php
            ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} "chmod 440 ~/JarabaImpactPlatformSaaS/web/sites/default/settings.local.php"
            rm -f /tmp/settings.local.php
            echo "✅ settings.local.php creado"
          else
            echo "❌ ERROR: No existe settings.local.php y no hay secrets IONOS_DB_*"
            echo "📋 Configura estos GitHub secrets: IONOS_DB_HOST, IONOS_DB_NAME, IONOS_DB_USER, IONOS_DB_PASS"
            echo "O crea settings.local.php manualmente en IONOS vía SSH"
            exit 1
          fi

      - name: Pull and deploy
        run: |
          ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} << 'EOF'
            cd ~/JarabaImpactPlatformSaaS

            # Desbloquear permisos para git (runbook §Deployment)
            chmod 755 web/sites/default
            chmod 644 web/sites/default/settings.php

            # Pull código (settings.local.php no se ve afectado — no está en git)
            git fetch origin
            git reset --hard origin/main

            # Habilitar RewriteBase (necesario en IONOS shared hosting)
            sed -i 's/# RewriteBase \//RewriteBase \//' web/.htaccess

            # Restaurar permisos seguros
            chmod 555 web/sites/default
            chmod 444 web/sites/default/settings.php

            # Verificar que settings.local.php existe post-reset
            if [ ! -f web/sites/default/settings.local.php ]; then
              echo "❌ FATAL: settings.local.php desapareció después de git reset"
              exit 1
            fi

            # Instalar y actualizar
            /usr/bin/php8.4-cli ~/bin/composer.phar install --no-dev --optimize-autoloader
            /usr/bin/php8.4-cli vendor/bin/drush.php updatedb -y
            /usr/bin/php8.4-cli vendor/bin/drush.php config:import -y || true
            /usr/bin/php8.4-cli vendor/bin/drush.php cache:rebuild
          EOF

      - name: Disable maintenance mode
        run: |
          ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }} << 'EOF'
            cd ~/JarabaImpactPlatformSaaS
            /usr/bin/php8.4-cli vendor/bin/drush.php state:set system.maintenance_mode 0
          EOF

      - name: Smoke test - Health endpoint
        run: |
          echo "⏳ Esperando 10s para estabilización post-deploy..."
          sleep 10
          
          echo "🔍 Verificando /health endpoint..."
          for i in 1 2 3; do
            RESPONSE=$(curl -sf https://plataformadeecosistemas.com/health 2>/dev/null || echo '{}')
            STATUS=$(echo "$RESPONSE" | jq -r '.status' 2>/dev/null || echo 'unknown')
            
            if [ "$STATUS" = "ok" ]; then
              echo "✅ Health check passed! Platform is healthy."
              echo "$RESPONSE" | jq .
              exit 0
            fi
            
            echo "⚠️ Intento $i/3: status=$STATUS. Reintentando en 5s..."
            sleep 5
          done
          
          echo "❌ Health check failed after 3 attempts!"
          echo "Response: $RESPONSE"
          exit 1

      - name: Smoke test - Homepage
        if: success()
        run: |
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://plataformadeecosistemas.com)
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "⚠️ Homepage returned HTTP $HTTP_STATUS (non-blocking)"
          else
            echo "✅ Homepage OK (HTTP 200)"
          fi

      - name: Notify success
        if: success()
        run: |
          echo "🎉 Deployment successful!"
          echo "Version: $(git describe --tags --always)"
          echo "Commit: ${{ github.sha }}"

      - name: Notify failure
        if: failure()
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "❌ *IONOS Deploy Failed*\n*Branch:* `${{ github.ref_name }}`\n*Commit:* `${{ github.sha }}`\n*Actor:* ${{ github.actor }}\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Logs>"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
        continue-on-error: true

      - name: Rollback instructions
        if: failure()
        run: |
          echo "❌ Deployment failed! Manual rollback may be required."
          echo ""
          echo "📋 Rollback steps:"
          echo "  1. SSH into IONOS: ssh ${{ env.IONOS_USER }}@${{ env.IONOS_HOST }}"
          echo "  2. Restore DB: gunzip -c ~/backups/db_pre_deploy_*.sql.gz | mysql"
          echo "  3. Revert code: cd ~/JarabaImpactPlatformSaaS && git reset --hard HEAD~1"
          echo "  4. Rebuild: composer install && drush cr"
